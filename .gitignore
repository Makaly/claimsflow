# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/
*.lcov
.nyc_output

# Production
build/
dist/
dist-ssr/
storybook-static/

# TypeScript incremental build cache
*.tsbuildinfo

# Generated OpenAPI spec — produced by EXPORT_OPENAPI=1
openapi.json

# Misc
.DS_Store
*.pem
.env
.env.local
.env.production
*.log

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Uploads & patient data — never commit
uploads/
uploaded_files/

# Database
*.db
*.sqlite

# Local Claude/agent harness state
.claude/
site/

# Generated audit and compliance reports (binary/HTML artifacts — store in secure drive)
*.pdf
audit_report.html

# Downloaded installer artifacts from testing the curl|bash one-liner
/claimsflow-install.sh
claimsflow-install.sh

# claimsflow-mobile is a SEPARATE git repo (remote: Makaly/claimsflow-mobile)
# checked out inside this working tree. Ignore it so the outer repo can never
# stage it as a gitlink. It is managed independently from its own .git.
/claimsflow/

# Android signing keystores — contain private keys, must never be tracked
*.jks
*.keystore

# Python — ml-sidecar
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.python-version
.pytest_cache/
.mypy_cache/
.ruff_cache/
