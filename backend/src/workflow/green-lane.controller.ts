// REMOVED — the green-lane auto-approval endpoints (rule CRUD + evaluate +
// global toggle + daily summary) have been deleted. Automatic approval of
// claims is no longer permitted; all approvals go through the human
// maker_checker → claims_officer workflow. This controller is no longer
// registered in WorkflowModule.
export {};
